services:
  web:
    build: .
    image: pdf-sucker:local
    ports:
      - "8080:80"
    volumes:
      # Persistência e RW explícitos para temp/compressed entre reinícios
      - uploads_data:/var/www/html/uploads
    restart: unless-stopped

volumes:
  uploads_data:
